<section class="notification js-notification notification--danger content">
    {% if messages %}
        {% for message in messages %}
            <p class="notification-content notification-content--danger">
                {{ message }}
            </p>
        {% endfor %}
    {% endif %}
</section>
