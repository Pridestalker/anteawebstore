<section class="notification js-notification notification--success content">
    {% if messages %}
        {% for message in messages %}
            <p class="notification-content notification-content--success">
                {{ message }}
            </p>
        {% endfor %}
    {% endif %}
</section>
